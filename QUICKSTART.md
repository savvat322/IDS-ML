# 🚀 БЫСТРЫЙ СТАРТ

Пошаговая инструкция по запуску системы обнаружения вторжений

---

## Шаг 1: Установка зависимостей

```bash
# Создание виртуального окружения (рекомендуется)
python -m venv venv

# Активация виртуального окружения
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt
```

---

## Шаг 2: Получение данных

### Вариант А: Скачивание реального датасета CIC-IDS2017

1. Посетите https://www.unb.ca/cic/datasets/ids-2017.html
2. Скачайте CSV файлы
3. Разместите их в `data/raw/`

### Вариант Б: Использование вспомогательного скрипта

```bash
python src/download_dataset.py
```

Выберите один из вариантов:
- Скачивание через Kaggle API (требуется регистрация)
- Создание образца данных для тестирования (рекомендуется для начала)

---

## Шаг 3: Предобработка данных

```bash
# Полная предобработка датасета
python src/data_preprocessing.py

# ИЛИ для быстрого теста (только 10000 строк из каждого файла)
python src/data_preprocessing.py --sample 10000
```

**Результат:** Обработанные данные будут сохранены в `data/processed/`

**Время выполнения:**
- Полный датасет: ~30-60 минут
- Sample (10K строк): ~2-5 минут

---

## Шаг 4: Обучение моделей

### 4.1 Random Forest

```bash
# С подбором гиперпараметров (медленно, но точно)
python src/train_random_forest.py

# ИЛИ без GridSearch (быстро)
python src/train_random_forest.py --no-grid-search
```

**Время выполнения:**
- С GridSearch: ~20-40 минут
- Без GridSearch: ~5-10 минут

### 4.2 Neural Network

```bash
python src/train_neural_network.py
```

**Время выполнения:** ~10-20 минут

**Результат:** Обученные модели сохранены в `models/`

---

## Шаг 5: Оценка моделей

```bash
python src/evaluation.py
```

**Результат:**
- Метрики моделей: `results/evaluation_results.json`
- Сравнительная таблица: `results/model_comparison.csv`
- Графики: `results/figures/`

---

## Шаг 6: Real-time IDS

### Режим симуляции (на тестовых данных)

```bash
python src/realtime_ids.py --mode simulate
```

### Live режим (захват реального трафика)

**⚠ Требуются права администратора!**

```bash
# Windows (запустите PowerShell от имени администратора)
python src/realtime_ids.py --mode live --interface "Ethernet"

# Linux/Mac
sudo python src/realtime_ids.py --mode live --interface eth0
```

**Остановка:** Нажмите `Ctrl+C`

**Логи:** `results/realtime_alerts.log`

---

## 🎯 Типичный рабочий процесс

### Первый запуск (полный цикл)

```bash
# 1. Создание sample данных
python src/download_dataset.py
# Выберите: 3 (Создать образец данных)

# 2. Предобработка
python src/data_preprocessing.py --sample 10000 --balance smote

# 3. Обучение Random Forest (быстро)
python src/train_random_forest.py --no-grid-search

# 4. Обучение Neural Network
python src/train_neural_network.py

# 5. Оценка
python src/evaluation.py

# 6. Тестирование Real-time IDS
python src/realtime_ids.py --mode simulate
```

**Общее время:** ~30-40 минут

---

## 📊 Jupyter Notebooks

Для интерактивного анализа:

```bash
# Запуск Jupyter
jupyter notebook

# Откройте notebooks в браузере:
# - notebooks/01_data_exploration.ipynb
# - notebooks/02_model_training.ipynb
# - notebooks/03_results_visualization.ipynb
```

---

## 🔧 Конфигурация

Настройки системы в файле `config.yaml`:

```yaml
# Изменение порога обнаружения атак
realtime:
  alert_threshold: 0.7  # 0.0-1.0, выше = меньше алертов

# Изменение метода балансировки
preprocessing:
  balance_method: "smote"  # smote, undersample, none

# Параметры нейронной сети
neural_network:
  epochs: 50
  batch_size: 256
  learning_rate: 0.001
```

---

## 📁 Структура результатов

После выполнения всех шагов:

```
results/
├── evaluation_results.json      # Метрики моделей
├── model_comparison.csv         # Сравнительная таблица
├── realtime_alerts.log          # Логи обнаруженных атак
└── figures/                     # Визуализации
    ├── metrics_comparison.png
    ├── confusion_matrices.png
    ├── roc_curves.png
    ├── rf_feature_importance.png
    └── nn_training_history.png
```

---

## ❓ Часто задаваемые вопросы

### Q: Датасет слишком большой, можно ли использовать меньше данных?

**A:** Да! Используйте параметр `--sample`:
```bash
python src/data_preprocessing.py --sample 5000
```

### Q: Обучение занимает слишком много времени

**A:** Отключите GridSearch:
```bash
python src/train_random_forest.py --no-grid-search
```

### Q: Ошибка "ModuleNotFoundError"

**A:** Убедитесь, что виртуальное окружение активировано и все зависимости установлены:
```bash
pip install -r requirements.txt
```

### Q: Real-time IDS не работает на Windows

**A:** 
1. Установите Npcap: https://npcap.com/#download
2. Запустите PowerShell от имени администратора
3. Используйте режим симуляции: `--mode simulate`

### Q: Как узнать имя сетевого интерфейса?

**A:**
```bash
# Windows
ipconfig

# Linux/Mac
ifconfig
# или
ip addr
```

---

## 🐛 Решение проблем

### Ошибка: "No CSV files found"

```bash
# Проверьте, что CSV файлы находятся в data/raw/
ls data/raw/

# Если нет, запустите download_dataset.py
python src/download_dataset.py
```

### Ошибка: "Processed data not found"

```bash
# Сначала выполните предобработку
python src/data_preprocessing.py --sample 10000
```

### Ошибка: "Model not found"

```bash
# Сначала обучите модели
python src/train_random_forest.py --no-grid-search
python src/train_neural_network.py
```

### Ошибка памяти (MemoryError)

```bash
# Используйте меньше данных
python src/data_preprocessing.py --sample 5000

# ИЛИ отключите балансировку
python src/data_preprocessing.py --balance none
```

---

## 📚 Дополнительная документация

- **Полная документация:** `docs/курсовая_записка.md`
- **Презентация:** `docs/презентация.md`
- **Доклад:** `docs/доклад_для_защиты.md`
- **README:** `README.md`

---

## 🎓 Для преподавателей

### Быстрая проверка работоспособности (5 минут)

```bash
# 1. Создание тестовых данных
python src/download_dataset.py  # Выбрать: 3

# 2. Предобработка (small sample)
python src/data_preprocessing.py --sample 5000 --balance none

# 3. Быстрое обучение
python src/train_random_forest.py --no-grid-search

# 4. Оценка
python src/evaluation.py

# 5. Real-time симуляция
python src/realtime_ids.py --mode simulate
```

Все должно работать без ошибок!

---

## 🤝 Поддержка

**Автор:** Тураев Сейит  
**Email:** turaev@example.com  
**Университет:** ГрГУ им. Я. Купалы  
**Год:** 2025

---

**Удачи в использовании системы! 🚀**

